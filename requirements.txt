# Build-time dependencies (needed for `python setup.py build_ext --inplace`;
# `pip install -e .` pulls these in automatically via pyproject.toml).
setuptools
cython
numpy
# Runtime dependencies
scipy>=1.5
scikit-learn
# Dev / examples / tests
pandas
tqdm
matplotlib
seaborn
joblib
pytest
pytest-cov
